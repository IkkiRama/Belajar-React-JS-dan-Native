import { StyleSheet, Text, View } from "react-native";
import React from "react";

const CobaNavigator = () => {
  return (
    <View>
      <Text>CobaNavigator</Text>
    </View>
  );
};

export default CobaNavigator;

const styles = StyleSheet.create({});
